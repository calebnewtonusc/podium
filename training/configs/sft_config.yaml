---
# Stage 1: Supervised Fine-Tuning config
# Run: torchrun --nproc_per_node=18 training/train.py --config training/configs/sft_config.yaml

model:
  base_model: "Qwen/Qwen2.5-7B-Coder-Instruct"
  output_dir: "./checkpoints/sft"
  max_seq_length: 16384
  torch_dtype: "bfloat16"

lora:
  r: 64
  lora_alpha: 128
  lora_dropout: 0.05
  target_modules:
    - q_proj
    - k_proj
    - v_proj
    - o_proj
    - gate_proj
    - up_proj
    - down_proj
  bias: "none"
  task_type: "CAUSAL_LM"

training:
  num_train_epochs: 3
  per_device_train_batch_size: 2
  gradient_accumulation_steps: 4
  learning_rate: 2.0e-4
  warmup_ratio: 0.03
  lr_scheduler_type: "cosine"
  weight_decay: 0.01
  max_grad_norm: 1.0
  bf16: true
  gradient_checkpointing: true
  packing: false  # Packing corrupts chat format (concatenates <|im_end|><|im_start|>)
  dataset_text_field: "text"

logging:
  logging_steps: 25
  save_steps: 500
  eval_steps: 500
  eval_strategy: "steps"
  save_total_limit: 3
  report_to: []
  run_name: "podium-sft-v1"

data:
  notebook_pairs: "./data/synthesized/notebook_pairs.jsonl"
  writeup_pairs: "./data/synthesized/writeup_pairs.jsonl"
  discussion_pairs: "./data/synthesized/discussion_pairs.jsonl"
  technique_pairs: "./data/synthesized/technique_pairs.jsonl"
  meta_pairs: "./data/synthesized/meta_competition_pairs.jsonl"
