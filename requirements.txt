# Podium Dependencies
# Python 3.11+ required

# ── Core ML ──────────────────────────────────────────────────────────────────
torch>=2.3.0
torchvision>=0.18.0
torchaudio>=2.3.0
transformers>=4.45.0
datasets>=2.20.0
tokenizers>=0.19.0
accelerate>=0.33.0
peft>=0.12.0                    # LoRA fine-tuning
deepspeed>=0.15.0               # ZeRO-3 training
trl>=0.10.0                     # SFT, DPO, GRPO trainers
bitsandbytes>=0.43.0            # 4-bit quantization

# ── Competition ML Stack ──────────────────────────────────────────────────────
scikit-learn>=1.5.0
lightgbm>=4.4.0
xgboost>=2.1.0
catboost>=1.2.5
optuna>=3.6.0                   # Hyperparameter optimization
timm>=1.0.0                     # CV architectures
albumentations>=1.4.0           # Image augmentation

# ── Data Processing ───────────────────────────────────────────────────────────
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.13.0
polars>=0.20.0                  # Fast dataframe alternative
pyarrow>=16.0.0
h5py>=3.11.0

# ── Training Infrastructure ───────────────────────────────────────────────────
wandb>=0.17.0                   # Experiment tracking
huggingface-hub>=0.24.0         # Model upload/download
safetensors>=0.4.3

# ── vLLM Synthesis ────────────────────────────────────────────────────────────
vllm>=0.5.0                     # Fast LLM inference for synthesis
openai>=1.35.0                  # vLLM OpenAI-compatible client

# ── Kaggle Data Collection ────────────────────────────────────────────────────
kaggle>=1.6.17                  # Kaggle API client
requests>=2.32.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
aiohttp>=3.9.0

# ── Deployment ────────────────────────────────────────────────────────────────
fastapi>=0.112.0
uvicorn[standard]>=0.30.0
redis>=5.0.0
websockets>=12.0

# ── Execution Harness ─────────────────────────────────────────────────────────
docker>=7.1.0                   # Python Docker SDK (CV-RL execution)
psutil>=6.0.0

# ── Vector Memory Store ───────────────────────────────────────────────────────
chromadb>=0.5.0                 # Persistent competition memory
sentence-transformers>=3.0.0    # Competition fingerprinting embeddings

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.8.0
tqdm>=4.66.0
rich>=13.7.0
typer>=0.12.0
loguru>=0.7.0
datasketch>=1.6.5               # MinHash deduplication
